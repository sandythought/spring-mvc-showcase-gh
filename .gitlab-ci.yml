image: maven:latest

variables:
  MAVEN_CLI_OPTS: "-s .m2/settings.xml --batch-mode"
  MAVEN_OPTS: "-Dmaven.repo.local=.m2/repository"

cache:
  paths:
    - .m2/repository/
    - target/

verify:
  stage: verify
  script:
    - mvn verify sonar:sonar -Dsonar.login=$SONAR_LOGIN
